# Railway: build and run from this folder (set Root Directory to yekermo-backend in the dashboard)
[build]
builder = "nixpacks"

[deploy]
startCommand = "npm run start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
